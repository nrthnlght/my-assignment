#include <stdio.h>
#include <stdlib.h>

int main ()
{
    int awal, akhir, i, hapus;

    printf ("masukkan kamar awal :");
    scanf ("%d", &awal);
    printf ("masukkan kamar awal :");
    scanf ("%d", &akhir);
    printf ("%d %d", awal, akhir );

    for (i= awal; i<= akhir ; i ++){
        if (i == 3)
          {
                scanf ("%d", hapus);
              system("pause");
          }
          else if

    }

}
